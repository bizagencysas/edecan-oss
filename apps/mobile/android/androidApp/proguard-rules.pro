# Reglas R8/ProGuard propias de Edecán — vacías por ahora: `isMinifyEnabled`
# está en `false` en el `release` de este esqueleto (ver build.gradle.kts),
# así que este archivo todavía no corre. Referenciado desde ya para que
# activar minify más adelante sea solo cambiar ese flag, sin tener que
# tocar la config del módulo. Ver también las reglas por defecto que ya
# aplica `proguard-android-optimize.txt` (AGP).
