<?xml version="1.0" encoding="utf-8"?>
<!--
    Solo los permisos que la app realmente usa (ROADMAP_V2.md §6.1):
    `INTERNET` para hablar con `/v1/*`, `CAMERA` únicamente para el escáner
    QR iniciado por la persona y `RECORD_AUDIO` para el push-to-talk
    del botón de micrófono dentro de Chat (`POST /v1/voice/transcribe`) — pedido en
    tiempo de ejecución desde `VozScreen`, este `<uses-permission>` solo lo
    declara. Nada de contactos/ubicación/notificaciones todavía.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- El QR también puede ingresarse como deep link/manual; no se excluyen
         tablets, Chromebooks ni equipos sin cámara de Google Play. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Visibilidad de paquetes exigida desde Android 11 para descubrir los
         servicios locales que dan voz sin claves de terceros. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Edecan"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true">
        <!--
            `network_security_config` mantiene HTTPS obligatorio para Internet
            y solo permite HTTP para nombres mDNS `*.local`: es el enlace
            directo, dentro de la misma red, con el Edecán de escritorio.
            Debug reemplaza el recurso para permitir también localhost/IPs de
            desarrollo. La excepción de producción no acepta dominios públicos.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Edecan"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="edecan" android:host="pair" />
            </intent-filter>
        </activity>

        <!-- Solo comparte archivos que la propia app acaba de descargar en
             cacheDir/shared_artifacts; nunca expone almacenamiento general. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
